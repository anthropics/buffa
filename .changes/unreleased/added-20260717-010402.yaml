kind: Added
body: |-
    **`DecodeOptions::with_element_memory_limit` bounds the memory a decode materializes in repeated message, string and bytes elements**, defaulting to 32 MiB (`DEFAULT_ELEMENT_MEMORY_LIMIT`) where it was previously unbounded (#301).
    These elements cost far more decoded than encoded, which no existing option bounded: an empty repeated message element is 2 bytes on the wire and `size_of::<T>()` in the `Vec` it lands in — measured at 256 bytes for a message of a few `Vec`/`String` fields, a 128x ratio, so 4 MiB of them forced ~512 MiB. `with_max_message_size` cannot help, because it bounds the bytes going in rather than what they expand into. Empty `bytes` and `string` elements amplify 16x and 12x by the same route.
    The budget is charged by element footprint and shared across the whole decode, so a limit means the same amount of memory whatever the element size. Packed scalar fields are never charged: their worst case is a 1-byte varint becoming a 4-byte `i32`, and bounding them would reject columnar payloads that carry millions of elements by design.
    **This can reject input that previously decoded.** A decode materializing more than 32 MiB of such elements now fails with `DecodeError::ElementMemoryLimitExceeded`; raise the budget with `with_element_memory_limit` for trusted input that legitimately decodes into more. Note `Vec` grows by doubling, so peak resident memory can reach roughly twice the budget — the budget bounds what is materialized, not what the allocator reserves.
time: 2026-07-17T01:04:02.091134+00:00
