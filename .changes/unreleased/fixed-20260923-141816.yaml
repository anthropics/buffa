kind: Fixed
body: |-
    **`MapValue::get_str` returns `None` for non-string keys in debug builds**, as documented, instead of panicking on an assertion. This also fixes string lookups through `ReflectMap` on integer- or boolean-keyed maps.
time: 2026-09-23T14:18:16Z
