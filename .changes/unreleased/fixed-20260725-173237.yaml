kind: Fixed
body: 'Reflective `DynamicMessage` JSON integer decoding now matches generated messages: quoted decimal and exponent forms parse exactly across the full `i64`/`u64` range, while unsafe high-magnitude unquoted floats are rejected before serde_json rounding can turn them into an adjacent integer.'
time: 2026-07-25T17:32:37.281642+02:00
