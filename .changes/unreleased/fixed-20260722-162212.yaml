kind: Fixed
body: 'Reflective JSON decoding now parses quoted decimal and exponential integer strings exactly, so `DynamicMessage::from_json` accepts valid 64-bit values above `i64::MAX`, including `u64::MAX`, without routing them through lossy `f64` conversion.'
time: 2026-07-22T16:22:12.000000000Z
